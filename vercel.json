{
  "framework": "vite",
  "outputDirectory": "dist",
  "functions": {
    "api/**": {
      "excludeFiles": "{cctv/**,config/**,model/**,shared/**,tdx/**,traffic/**,*.ts}"
    }
  },
  "rewrites": [
    { "source": "/((?!api/.*).*)", "destination": "/index.html" }
  ]
}
